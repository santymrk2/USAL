#include <stdio.h>

int cubo (int base) {
	int result;
	result = base*base*base;
	return resultado;
}

int main() {
	//leer
	int n;
	puts("Ingrese un numero");
	scanf("%d", &n);
	//imprimir
	printf("El cubo es: %d", cubo(n));
	return 0;
}
