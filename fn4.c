#include <stdio.h>

int main(){
    int can=1;
    double mont[can], total=0;
    char mp[can];
    for(int i=0;i<can;i++){
        do{
            puts("Ingrese el medio de pago (E=efectivo, D=debito, C=credito)");
            scanf("%lf", &mont[i]);
        } while(mont[i]!=0 && mont[i]<0);
        if
    }
    
}
